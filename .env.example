# Environment Variables Template

## Frontend (.env.local)
```bash
# API Configuration
NEXT_PUBLIC_API_URL=https://your-backend.vercel.app

# Better Auth Configuration
BETTER_AUTH_SECRET=your-secret-key-here-generate-using-openssl
BETTER_AUTH_URL=http://localhost:3000

# Database (PostgreSQL)
DATABASE_URL=postgresql://user:password@host:port/database

# Optional: Email Configuration (for Better Auth)
RESEND_API_KEY=your-resend-api-key
```

## Backend (.env)
```bash
# Database Configuration
DATABASE_URL=postgresql://user:password@host:port/database

# Better Auth (if needed for backend validation)
BETTER_AUTH_SECRET=same-secret-as-frontend
BETTER_AUTH_URL=https://your-frontend-domain.com

# Optional: CORS Origins (comma-separated)
CORS_ORIGINS=https://your-frontend-domain.com,http://localhost:3000
```

## GitHub Secrets (for CI/CD)

Add these secrets in your GitHub repository:
- Go to: Settings → Secrets and variables → Actions → New repository secret

Required secrets:
- `NEXT_PUBLIC_API_URL`: Your Vercel backend URL
- `BETTER_AUTH_SECRET`: Generate with `openssl rand -base64 32`
- `BETTER_AUTH_URL`: Your frontend URL
- `DATABASE_URL`: Your PostgreSQL connection string

## Vercel Environment Variables

Add these in your Vercel project settings:
- Dashboard → Your Project → Settings → Environment Variables

Required for backend:
- `DATABASE_URL`: Your PostgreSQL connection string
- `BETTER_AUTH_SECRET`: Same as frontend
- `BETTER_AUTH_URL`: Your frontend URL

## How to Generate Secrets

### BETTER_AUTH_SECRET
```bash
openssl rand -base64 32
```

### DATABASE_URL Format
```
postgresql://username:password@hostname:port/database
```

## Database Options (Free Tier)

1. **Neon** (Recommended): https://neon.tech
   - Free PostgreSQL
   - Serverless
   - Easy setup

2. **Supabase**: https://supabase.com
   - Free PostgreSQL
   - Additional features

3. **ElephantSQL**: https://www.elephantsql.com
   - Free tier available
